This is your new Play application
=================================

This file will be packaged with your application when using `activator dist`.

There are several demonstration files available in this template.

Controllers
===========

- HomeController.%LANG_FILE_SUFFIX%:

  Shows how to handle simple HTTP requests.

- AsyncController.%LANG_FILE_SUFFIX%:

  Shows how to do asynchronous programming when handling a request.

- CountController.%LANG_FILE_SUFFIX%:

  Shows how to inject a component into a controller and use the component when
  handling requests.

Components
==========

- Module.%LANG_FILE_SUFFIX%:

  Shows how to use Guice to bind all the components needed by your application.

- Counter.%LANG_FILE_SUFFIX%:

  An example of a component that contains state, in this case a simple counter.

- ApplicationTimer.%LANG_FILE_SUFFIX%:

  An example of a component that starts when the application starts and stops
  when the application stops.

Filters
=======

- Filters.%LANG_FILE_SUFFIX%:

  Creates the list of HTTP filters used by your application.

- ExampleFilter.%LANG_FILE_SUFFIX%

  A simple filter that adds a header to every response.