# Play sometimes gets grumpy if this file doesn't exist.
play.crypto.secret = "abc"

actor {
  default-dispatcher = {
    fork-join-executor {
      parallelism-max = 2
    }
  }
}

// we should do our integration tests with fuzzing-mode
// to find rare race condition bugs
akka.stream.materializer.debug.fuzzing-mode = on